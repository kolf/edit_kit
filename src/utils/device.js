export const getDevice = () => {
  const dimensionMap = {
    xs: '480px',
    sm: '768px',
    md: '992px',
    lg: '1200px',
    xl: '1600px'
  }

  // matchMedia('(max-width: ' + dimensionMap[props.breakpoint] + ')')

  // if (window) {
  //   return window.matchMedia
  // }

}